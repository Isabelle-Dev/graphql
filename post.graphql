query ToolDemo {
    tool {
        query(query: "uses: \"100\" variation: \"blue OR red\" ") {
            tools {
                Name
                Uses
                Variants {
                    Variation
                }
            }
        }
    }
}